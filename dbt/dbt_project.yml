name: modern_retail_elt_warehouse
version: "1.0.0"
config-version: 2

profile: modern_retail_elt_warehouse

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  modern_retail_elt_warehouse:
    staging:
      +materialized: view
      +schema: staging